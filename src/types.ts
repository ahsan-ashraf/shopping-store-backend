export enum Role {
  SuperAdmin = "SuperAdmin",
  Admin = "Admin",
  Seller = "Seller",
  Rider = "Rider",
  Buyer = "Buyer"
}

export enum Gender {
  male,
  female,
  other
}

export enum Status {
  pendingApproval = "pendingApproval",
  active = "active",
  blocked = "blocked"
}
